export default function HelpModal({ onClose }) {
  return (
    <div className="merge-modal-backdrop" onClick={onClose}>
      <div className="help-modal" onClick={(e) => e.stopPropagation()}>
        <div className="merge-modal-header">
          <h3>사용 방법</h3>
          <button className="llm-submit secondary" onClick={onClose}>닫기</button>
        </div>

        <div className="help-body">
          <section>
            <h4>0. 로그인</h4>
            <ul>
              <li>처음이면 "회원가입" — 이름/아이디/접속 코드/이메일/비밀번호 입력. 접속 코드는 팀 관리자에게 따로 받아야 함
                (외부인이 마음대로 가입 못 하게 막는 용도)</li>
              <li>회원가입과 로그인은 별개 — 가입 완료되면 자동으로 들어가지지 않고, 로그인 화면으로 돌아가서 아이디/비밀번호로
                직접 로그인해야 함</li>
              <li>로그인 정보는 이 사이트 서버에만 저장되고, 팀원마다 각자 계정으로 접속</li>
              <li>2.5시간 동안 아무 조작이 없으면 자동으로 로그아웃됨 (보안용)</li>
            </ul>
          </section>

          <section>
            <h4>1. 프로젝트 선택</h4>
            <ul>
              <li>로그인하면 내가 속한 프로젝트 목록이 먼저 나옴 — 클릭하면 그 프로젝트로 들어감</li>
              <li><b>내가 멤버로 초대된 프로젝트만 보임.</b> 다른 사람이 만든 프로젝트는 초대받기 전까지 목록에도 안 뜨고,
                주소를 알아도 못 들어감</li>
              <li>"+ 새 프로젝트"로 새로 만들 수 있음 — 이름만 입력하면 되고, 만든 사람이 자동으로 첫 멤버가 됨</li>
              <li>왼쪽 위 이름 옆 "⋯" 더보기 메뉴 안에 "프로젝트 변경"(다른 프로젝트로 이동)과 "멤버 초대"가 있음 —
                초대는 프로젝트 멤버라면 누구나 가능하고, 초대되면 그 사람 목록에도 프로젝트가 나타남</li>
              <li>브랜치 목록 아래 "▸ 현재 프로젝트 참여자" 를 누르면 지금 이 프로젝트에 속한 멤버 목록이 펼쳐짐 —
                다른 멤버를 제외하는 건 관리자만 가능하고, 본인은 언제든 스스로 나갈 수 있음(자기 이름 옆 × 버튼)</li>
              <li>프로젝트마다 브랜치·파일·히스토리가 완전히 독립적임</li>
            </ul>
          </section>

          <section>
            <h4>2. 파일 편집 &amp; 시뮬레이션</h4>
            <ul>
              <li>왼쪽 "파일" 탭에서 .v 파일 클릭하면 에디터에 열림</li>
              <li>편집하면 0.8초 후 자동 저장 (내 브랜치일 때만 가능)</li>
              <li>상단 버튼은 <b>지금 에디터에 열려있는 파일에 따라 자동으로 바뀜</b> — 테스트벤치를 열면 "Run Simulation"
                (그 테스트벤치 실행), RTL 파일을 열면 "컴파일 확인"(테스트벤치 없이 컴파일만 체크)</li>
              <li>다른 파일을 굳이 열지 않고도, 파일 목록에서 테스트벤치 옆 "▶"나 RTL 옆 "✓"를 눌러 바로 실행/확인할 수 있음</li>
              <li>"Run Simulation" → 컴파일 에러 / PASS / FAIL / 결과 불명이 아래 로그에 표시됨</li>
              <li>"새 파일"(+) / 파일 옆 × 로 생성·삭제, ✎로 이름 변경</li>
              <li>파일 옆 🗄로 "보관" — 삭제하지 않고 파일 목록/시뮬레이션/내보내기(zip)에서만 안 보이게 치워둠. 파일 목록
                맨 아래 "▸ 보관된 파일 (N)"을 펼치면 보관된 파일들을 볼 수 있고, "복원"으로 언제든 다시 꺼낼 수 있음</li>
              <li>파일 목록 위 전체/RTL/TB/BRAM 필터로 원하는 종류만 골라볼 수 있음</li>
              <li>왼쪽 "히스토리" 탭 맨 위에 "최근 시뮬레이션 실행" 목록이 있어서, 최근에 어떤 테스트벤치를 누가 언제 돌렸고
                PASS/FAIL이었는지 볼 수 있음 (실제 커밋과는 별개로 기록됨)</li>
              <li>모바일에서는 브랜치 / 파일 / 히스토리 / 에디터 / LLM 이 각각 별도 탭으로 나뉘어 있음 (화면 위쪽)</li>
            </ul>
          </section>

          <section>
            <h4>3. 주의사항</h4>
            <ul>
              <li><b>TB/BRAM 태그는 파일 내용이 아니라 파일 이름만 보고 자동으로 붙습니다</b> — 서버가 코드를 분석하는 게 아니라
                이름이 정해진 규칙에 맞는지만 확인해요
                <ul>
                  <li>이름 어딘가에 <code>_tb</code>가 들어가면(<code>~_tb.v</code>, <code>tb_~.v</code> 등) → <span className="badge badge-testbench">TB</span></li>
                  <li>이름이 <code>_숫자x숫자.v</code>로 끝나면(앞부분은 상관없음, 예: <code>state_bram_432x171.v</code>,
                    <code>내이름_576x512.v</code>) → <span className="badge badge-bram">BRAM</span></li>
                  <li>둘 다 아니면 → <span className="badge badge-rtl">RTL</span></li>
                </ul>
              </li>
              <li>그래서 이름만 규칙에 맞으면 실제 내용과 상관없이 태그가 붙어요 — 파일 종류를 강제하는 게 아니라, 사람이 한눈에
                구분하기 쉽게 해주는 표시일 뿐입니다</li>
              <li><span className="badge badge-bram">BRAM</span> 태그가 붙은 파일은 보통 Vivado Block Memory Generator IP를
                시뮬레이션용으로 흉내낸 동작(behavioral) 스텁 코드예요. Icarus Verilog 같은 오픈소스 시뮬레이터로 기능 검증만
                하기 위한 용도로, 실제 Xilinx IP(.xci)나 UNISIM 시뮬레이션 라이브러리를 대체하지 않습니다</li>
              <li>실제 합성 / 비트스트림 생성 시에는 이런 스텁 파일을 반드시 제외하고, Vivado에서 생성한 진짜 IP 코어로 교체해야 합니다</li>
            </ul>
          </section>

          <section>
            <h4>4. 브랜치 (각자 작업 공간)</h4>
            <ul>
              <li>왼쪽 "브랜치" 목록 — 클릭하면 그 브랜치 코드를 열람</li>
              <li>"+" 로 새 브랜치 생성 → 만든 사람 계정이 자동으로 "소유자"가 됨</li>
              <li>내 브랜치가 아닌 곳은 읽기 전용 (코드·이력·로그는 볼 수 있지만 편집 불가) — 서버에서 실제로 막혀 있음</li>
              <li>소유자가 없는(예전에 만든) 브랜치는 "내 브랜치로 표시"로 내가 가져올 수 있음. 이미 다른 사람 소유면 가져올 수 없음</li>
              <li>"⋯" 더보기 메뉴 안에 "협업자 추가"로 다른 사람을 이 브랜치에 초대할 수 있음 (소유자 또는 관리자만) — 협업자는
                소유자와 동일하게 편집·시뮬레이션·되돌리기·main으로 merge까지 가능</li>
              <li><b>"협업자 추가"(브랜치 단위 편집 권한)와 위 1번의 "멤버 초대"(프로젝트 단위 출입 권한)는 서로 다름</b> —
                프로젝트 멤버가 아니면 애초에 그 프로젝트에 들어올 수조차 없고, 브랜치 협업자는 이미 들어온 사람 중 누가
                특정 브랜치를 편집할 수 있는지를 정하는 것</li>
              <li>협업 중인 브랜치는 "협업 중" 배지로 표시됨. 협업자 본인은 언제든 스스로 빠질 수 있고, 소유자/관리자는 아무 협업자나 제외 가능</li>
              <li>"⋯" 메뉴의 "넘기기"로 브랜치 소유권 자체를 다른 사람에게 완전히 이전할 수 있음 (소유자 또는 관리자만)</li>
              <li>"main 반영" 버튼 — main의 최신 내용을 지금 이 브랜치로 가져와 합침 (merge의 반대 방향). 충돌 나면
                아래 "7. Merge"와 똑같은 방식으로 충돌 해결 창이 뜸</li>
              <li><b>main은 아무도 직접 못 고침</b> — merge로만 반영됨</li>
            </ul>
          </section>

          <section>
            <h4>5. 히스토리 &amp; 되돌리기</h4>
            <ul>
              <li>왼쪽 "히스토리" 탭 — 저장/커밋될 때마다 자동으로 버전이 남음</li>
              <li>커밋 메시지 끝에 "(by 이름)"이 자동으로 붙어서 누가 그 변경을 했는지 알 수 있음 (main 포함 전 브랜치 공통)</li>
              <li>시뮬레이션 돌린 적 있는 커밋은 PASS/FAIL 배지가 같이 보임</li>
              <li>상단 "파일" 드롭다운으로 특정 파일에 영향을 준 커밋만 필터링해서 볼 수 있음</li>
              <li>시작일/종료일로 기간 필터링도 가능 (파일 필터와 함께 사용 가능)</li>
              <li>"이 버전으로 되돌리기" → 그 시점 브랜치 전체 코드로 복원 (기록은 안 지워지고 새 커밋으로 쌓임)</li>
              <li>파일 필터가 켜져 있으면 버튼이 "이 파일만 되돌리기"로 바뀌며, 전체가 아니라 그 파일 하나만 해당 시점으로
                복원함 (다른 파일은 영향 없음)</li>
              <li>일반 사용자는 main을 되돌릴 수 없지만, <b>관리자는 예외적으로 main도 되돌리기 가능</b> — 누군가 잘못된
                내용을 merge했을 때 복구하는 용도</li>
            </ul>
          </section>

          <section>
            <h4>6. LLM(ChatGPT/Claude 등)에게 코드 수정 요청</h4>
            <ul>
              <li>오른쪽 패널에 요청 입력 (예: "이 에러 고쳐줘") → "프롬프트 복사"</li>
              <li>체크박스로 고른 파일들 + 최근 시뮬레이션 로그(에러 있으면)가 자동으로 프롬프트에 포함됨</li>
              <li>복사한 걸 ChatGPT나 Claude 같은 사이트에 직접 붙여넣고 답변 받기 (API 비용 없음, 무료)</li>
              <li>답변을 파일로 받았으면 "파일 업로드", 텍스트로 받았으면 "텍스트로 붙여넣기"</li>
              <li>파일별로 "복사"(클립보드만) 또는 "적용"(저장 + 자동 커밋)</li>
              <li>새 모듈이 없다는 에러(Unknown module)면 LLM이 새 파일을 만들어 줄 수도 있음 — 그것도 자동 인식됨</li>
              <li>내 브랜치가 아니면(읽기 전용) 이 패널은 비활성화됨 — 코드/이력/로그는 그대로 볼 수 있음</li>
              <li>화면이 좁으면 패널 오른쪽 위 "접기 ›" 버튼으로 패널을 접어서 에디터 공간을 넓힐 수 있음 (접은 상태는
                다음 방문 때도 그대로 유지됨)</li>
            </ul>
          </section>

          <section>
            <h4>7. Merge (내 브랜치를 main에 합치기)</h4>
            <ul>
              <li>왼쪽 브랜치 목록에서 내 브랜치의 "merge" 클릭</li>
              <li>충돌 없으면 바로 완료</li>
              <li>충돌 나면 창이 뜨고: 파일별로 충돌 마커(&lt;&lt;&lt;&lt;&lt;&lt;&lt; ======= &gt;&gt;&gt;&gt;&gt;&gt;&gt;)가 있는 코드를 직접 편집해서 정리
                <ul>
                  <li>참고용으로 main(ours) / 내 브랜치(theirs) / 공통 조상 버전을 오른쪽에서 토글해서 볼 수 있음</li>
                  <li>"LLM에게 병합 도움 요청"으로 프롬프트 복사해서 물어보고 답 붙여넣기도 가능</li>
                </ul>
              </li>
              <li>파일마다 "이 파일 저장" → 전부 해결되면 "병합 완료"</li>
              <li>"병합 취소"로 언제든 되돌릴 수 있음 (지금까지 해결한 내용은 버려짐)</li>
            </ul>
          </section>

          <section>
            <h4>8. 피드백 / 버그 제보</h4>
            <ul>
              <li>오른쪽 LLM 패널 맨 아래 "피드백 / 버그 제보"를 펼치면 분류(버그/제안/기타)를 고르고 내용을 적어 보낼 수 있음</li>
              <li>지금 보고 있던 프로젝트 정보가 자동으로 같이 전송됨</li>
              <li>관리자는 "관리자" 패널에서 들어온 피드백을 모아서 확인하고 처리 완료 표시를 할 수 있음</li>
            </ul>
          </section>
        </div>
      </div>
    </div>
  );
}
